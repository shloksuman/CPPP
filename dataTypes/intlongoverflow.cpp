#include<bits/stdc++.h>
using namespace std;

int main(){
    //  CASE 1:


    // int a = 1e5;
    // int b = 1e5;
    // cout << "a : " << a << " and b : " << b << endl;

    // int result;

    // result = a*b;

    // cout << "result = a*b = " << result << endl;


    // ------------------------------------
    // CASE 2:

    // int a = 1e5;
    // int b = 1e5;
    // cout << "a : " << a << " and b : " << b << endl;

    // long int result;

    // result = a*b;

    // cout << "result = a*b = " << result << endl;
    // ------------------------------------
    // CASE 3:

    // int a = 1e5;
    // int b = 1e5;
    // cout << "a : " << a << " and b : " << b << endl;

    // long int result;

    // result = (long int)a*b;

    // cout << "result = a*b = " << result << endl;
    // ------------------------------------
    // CASE 3:

    int a = 1e5;
    int b = 1e5;
    cout << "a : " << a << " and b : " << b << endl;

    long long int result;

    result = (long long int)a*b;

    cout << "result = a*b = " << result << endl;


    return 0;
}